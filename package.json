{
  "name": "babylon-pilot",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "smoke": "node smoke.mjs"
  },
  "dependencies": {
    "@babylonjs/core": "^8.0.0"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
